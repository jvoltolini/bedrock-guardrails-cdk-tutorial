# AWS Region (Bedrock Guardrails available regions)
AWS_DEFAULT_REGION=us-east-1

# Model ID (default: Amazon Nova Lite - no model access request needed)
# Alternative: anthropic.claude-3-5-haiku-20241022-v1:0 (requires model access)
MODEL_ID=us.amazon.nova-2-lite-v1:0

# AWS Profile (optional, if using named profiles)
# AWS_PROFILE=default
